<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:View.ViewModel"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800">

    <Window.DataContext>
        <vm:MainVM/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <GridSplitter Grid.Column="1" Width="3" HorizontalAlignment="Left"/>

        <Grid Grid.Column="0" Background="#f8f8f8">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="25"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Label Name="FindLabel" Content="Find: " HorizontalAlignment="Left" VerticalAlignment="Top" Margin="8,0,0,0"/>
                    <TextBox x:Name="SearchTextBox" VerticalAlignment="Top" Margin="44,4,8,0"/>
                </Grid>

                <Grid Grid.Row="1">
                    <ListBox x:Name="ContactsListBox" Margin="8"/>
                </Grid>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" x:Name="AddButton" Content="Add" Margin="8,0,3,0"/>
                    <Button Grid.Column="1" x:Name="EditButton" Content="Edit" Margin="3,0,3,0"/>
                    <Button Grid.Column="2" x:Name="RemoveButton" Content="Remove" Margin="3,0,8,0"/>

                </Grid>
            </Grid>
        </Grid>

        <Grid Grid.Column="2" Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Frame Grid.Column="0" Width="100" Height="100" Background="LightGray" Margin="8" VerticalAlignment="Top"/>

            <StackPanel Grid.Column="1">

                <Label Grid.Column="1" Name="NameLabel" Content="Name: " HorizontalAlignment="Left" VerticalAlignment="Top" Margin="3"/>
                <TextBox x:Name="NameTextBox" Margin="3"/>

                <Label Grid.Column="1" Name="PhoneNumberLabel" Content="Phone Number: " HorizontalAlignment="Left" VerticalAlignment="Center" Margin="3"/>
                <TextBox x:Name="PhoneNumberTextBox"  Margin="3"/>

                <Label Grid.Column="1" Name="EmailLabel" Content="Email: " HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="3"/>
                <TextBox x:Name="EmailTextBox"  Margin="3"/>

                <Button Grid.Column="1" Content="Apply" HorizontalAlignment="Left" Width="75" Height="25" Margin="3,50,0,0"/>
            </StackPanel>
        </Grid>

    </Grid>

</Window>
