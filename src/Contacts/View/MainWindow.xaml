<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:View.ViewModel"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        Title="MainWindow" Height="519" Width="646">

    <Window.DataContext>
        <vm:MainVM/>
    </Window.DataContext>

    <DockPanel Margin="15">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="75" Height="25" Margin="3" Command="{Binding SaveCommand}"/>
            <Button Content="Load" Width="75" Height="25" Margin="3" Command="{Binding LoadCommand}"/>
        </StackPanel>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="Edit Contact" Grid.Column="0" Margin="3">
                <StackPanel Margin="3">
                    <Label Content="Name:"/>
                    <TextBox Text="{Binding Name, Mode = TwoWay, UpdateSourceTrigger = PropertyChanged}" Margin="3"/>
                    <Label Content="Phone Number:"/>
                    <TextBox Text="{Binding PhoneNumber, Mode = TwoWay, UpdateSourceTrigger = PropertyChanged}" Margin="3"/>
                    <Label Content="Email:"/>
                    <TextBox Text="{Binding Email, Mode = TwoWay, UpdateSourceTrigger = PropertyChanged}" Margin="3"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Read Contact" Grid.Column="1" Margin="3">
                <StackPanel>
                    <Label Content="Name:"/>
                    <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="3"/>
                    <Label Content="Phone Number:"/>
                    <TextBlock Text="{Binding PhoneNumber}" FontWeight="Bold" Margin="3"/>
                    <Label Content="Email:"/>
                    <TextBlock Text="{Binding Email}" FontWeight="Bold" Margin="3"/>
                </StackPanel>
            </GroupBox>
        </Grid>
    </DockPanel>

</Window>
